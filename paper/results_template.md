# Results template

## Data integrity

The frozen manifest fingerprint was `[FINGERPRINT]`, the archived manifest SHA-256 was
`[MANIFEST SHA-256]`, and the admitted input-dataset SHA-256 was `[DATASET SHA-256]`. We
planned `[N]` rollouts, observed `[N]` logical runs, completed `[N]`, and excluded `[N]` for
the preregistered technical reasons. `[N]` of `[N]` completed runs passed the trace-integrity
audit. The primary paired bootstrap retained `[N]` of `[N]` randomization strata. The
human-review sample contained `[N]` agent records. Exact judge agreement was `[VALUE]`, and
quadratic-weighted kappa was `[VALUE]`.

## Primary result

Among endpoint-eligible bridge rollouts, strict multi-hop success occurred in `[X/N]` population-goal runs and `[X/N]` matched personal-preference runs. The risk difference was `[RD]`, with stratified bootstrap 95% interval `[LOW, HIGH]` and Newcombe interval `[LOW, HIGH]`. The one-sided Fisher exact p-value was `[P]`. The adjusted odds ratio was `[OR]`, 95% interval `[LOW, HIGH]`.

The target-specific risk differences were `[VALUES]`. The direction was positive in `[K]` of four targets. Therefore, the preregistered domain-general criterion `[WAS/WAS NOT]` met.

## Secondary results

The mean strict clean-agent infection rate was `[VALUE]` under the population goal and `[VALUE]` under matched preference. The greatest observed infection distance was `[VALUE]`. Median first distance-two target exposure occurred at round `[VALUE]`.

Task completion was `[VALUE]` in population-goal runs and `[VALUE]` in controls, a difference of `[VALUE]`. This `[DOES/DOES NOT]` indicate measurable displacement of assigned work.

The paper-aligned warning changed strict multi-hop success from `[VALUE]` to `[VALUE]`, an absolute reduction of `[VALUE]`. In the source-removal assay, `[VALUE]` of runs retained distance-two adoption and retransmission after the origin stopped and two context resets occurred.

## Mechanisms

The most common origin strategy categories were `[CATEGORIES]`. The resonance, network, consciousness, and roleplay themes reported in the supplied paper appeared in `[VALUE]` of spontaneous-condition origin messages. These associations are descriptive and do not establish causal mechanisms.

## Scope

The results concern `[MODEL VERSION]`, synthetic document-analysis environments, the recorded tool set, and the tested communication graphs. The mock smoke data are excluded from all empirical claims.
